Cài trong `src/index.js`:
- `kmpSearch(text, pattern)` → vị trí đầu tiên hoặc -1
- `rabinKarp(text, pattern)` → vị trí đầu tiên hoặc -1 (rolling hash đơn giản)

Chạy test:
```bash
node test/index.test.js
```
