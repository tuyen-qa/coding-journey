import assert from 'node:assert/strict'; import { all, any, race } from '../src/index.js'; const a=await all([Promise.resolve(1),Promise.resolve(2)]); assert.deepEqual(a,[1,2]); const b=await any([Promise.reject('e'),Promise.resolve(3)]); assert.equal(b,3); const r=await race([new Promise(r=>setTimeout(()=>r(5),5)), Promise.resolve(4)]); assert.equal(r,4); console.log('JS-06 OK');