'use strict';

/**
 * Skeleton: pipe(obj, ...fns) ➜ methodize to .pipe(...)
 * - pipe(obj, ...fns): run fns in order, each fn(obj), return obj
 * - pipeMethod: methodized version for chaining
 */

import { methodize } from './methodize_unmethodize_v2.js';

/** TODO: implement pipe(obj, ...fns) */
export function pipe(obj, ...fns){
  // TODO: for (const fn of fns) fn(obj); return obj;
}

/** TODO: pipeMethod = methodize(pipe) */
export const pipeMethod = null; // TODO
